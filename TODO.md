Make this configurable:
// CHildren will have good socket autopmaticly
	setenv("WAYLAND_DISPLAY", compositor->socket, true);
And the defoult layer, workspace, etc.

Make better logging and make it so there are logs like files and yk.

Find out why vtwm takes 100M of ram and if that's the foult of it being nested while testing(I haven't testet it as root comp cuz it doesn't have input so I don't have how to check yet)
